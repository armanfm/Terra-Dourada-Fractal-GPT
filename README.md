# Terra-Dourada-Fractal-GPT
Terra Dourada is a deterministic semantic memory engine that performs recall without embeddings or external models, using structural similarity, token semantics, and sovereign hashing to produce auditable, reproducible results.
